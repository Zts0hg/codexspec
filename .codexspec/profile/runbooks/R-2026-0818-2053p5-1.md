### R-2026-0818-2053p5-1: Complete a high-risk implement-tasks review when the primary reviewer cannot spawn specialists

- claim: When a high-risk final review needs independent specialists but the primary isolated reviewer cannot create them, complete specialists from the root context and aggregate them with a fresh final coordinator.
- type: runbook
- scope/when: `$codexspec:implement-tasks` final review gates that activate high-risk `review-code` profiles and return `INCONCLUSIVE` only because specialist topology could not be created inside the primary review context
- steps: 1) keep the implementation frozen and re-check the diff fingerprint; 2) preserve the primary isolated review output, but do not treat its prose as PASS; 3) spawn separate read-only specialist agents from the root context, partitioned by activated risk profiles; 4) collect only `state=complete` specialist reports with admitted P0-P3 findings and gaps; 5) rerun or cite post-freeze deterministic verification against the same fingerprint; 6) spawn a fresh read-only final coordinator to validate the primary report, specialist reports, verification, and frozen target, then emit the only final `review-code` envelope; 7) accept success only when that envelope is `PASS` with complete coverage, complete verification, zero P0-P3, zero blocking gaps, isolated context, and complete specialist topology.
- failure-recovery: if a reviewer or coordinator hits a usage limit before producing a legal envelope, do not edit or infer success; after the user resets/authorizes continuation, re-check the frozen fingerprint and retry only the missing review/coordinator step. If a specialist reports an actionable P0-P3 finding, independently verify and repair it before starting a new complete-feature review round.
- evidence.facts: "重置复审重试计数并再跑一次完整 isolated review，直到复审没有检查出问题"; "刚才额度不够，现在额度重置了。请你从之前的进度继续。"
- evidence.state: confirmed during feature `2026-0818-2053p5-reverse-spec`; primary review found zero findings but lacked callable specialist delegation, then three root-spawned specialists completed with zero P0-P3 findings and a fresh coordinator returned a valid PASS envelope.
- provenance: distill @implement-tasks, 2026-08-29, derivation: inferred, confidence: high
- status: candidate
