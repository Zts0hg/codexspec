### P-2026-0812-2114vj-1: Changing a chain command's auto_next successor requires regenerating derived forms during the feature

- claim: When you change a pipeline chain command's `## Auto-Next Chain Advance` successor (or insert a new chain stage), you MUST (1) update the `_AUTO_NEXT_SUCCESSOR` map in `tests/test_sdd_workflow_templates.py`, and (2) regenerate the derived install artifacts by running `uv run codexspec init . --force --ai both` *during the feature* (not deferring to release) — because `test_auto_next_section_synced_across_distribution_forms` reads the derived `.claude/commands/codexspec/<cmd>.md` and `.agents/skills/codexspec-<cmd>/SKILL.md` and asserts each names its successor. Regenerating via `init` is the legitimate self-bootstrap sync (NOT a hand-edit); the "sync derived at release" step is a catch-all, but this test goes red mid-feature until derived is regenerated.
- type: pitfall
- scope/when: editing a chain command's auto_next successor, or inserting/removing an SDD pipeline stage, in the codexspec repo
- evidence.facts: full suite failed with `test_auto_next_section_synced_across_distribution_forms[generate-spec]` → "generate-spec template missing successor spec-to-plan" and `test_chain_commands_have_auto_next_advance[generate-spec]` after generate-spec's auto_next was retargeted to spec-to-design; the sync test reads `.claude/commands/codexspec/` and `.agents/skills/` copies. Both went green only after updating `_AUTO_NEXT_SUCCESSOR` and running `codexspec init --force --ai both` to regenerate the 9 derived command + 9 skill forms.
- evidence.state: observed at feature 2026-0812-2114vj-spec-to-design (inserting the design stage), base commit 8e2aea6. Verified — suite green (1117 passed) after derived regen. First feature to change a chain successor, so prior features never tripped this. See [[Con-2026-0811-1418yq-1]] (lockstep counts) and [[Con-2026-0812-2114vj-1]] (catalog frontmatter drift) for the sibling distribution-surface obligations.
- provenance: distill @implement-tasks, 2026-08-12, derivation: inferred
- status: candidate
