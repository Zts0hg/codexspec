## D-2026-0812-14054p-2: profile store is one-file-per-record; consumption is uniform pointers (no @import)

- claim: The profile store is one record per file under a per-category directory (`pitfalls/<id>.md`, etc.), and both CLAUDE.md and AGENTS.md consume it via an identical **pointer** block (including constraints) with **no `@import`**. Parallel feature branches then add differently-named files that git merges with zero conflict, and the block depends on no tool-specific import mechanism.
- type: decision
- scope/when: the `.codexspec/profile/` store layout and how init injects the profile block into context files
- evidence.facts: "换成一记录一文件。然后CLAUDE.md改为跟AGENTS.md一样全部使用指针的形式"; "琐碎是贬义词……这也是我一直不想采用冲突再合并的方式，因为这样给用户带来了麻烦的操作，影响了用户体验". Single dense files caused an add/add merge conflict between parallel branches; ID namespacing removed the id clash but not the textual conflict; imposing any manual merge on users is a UX defect.
- evidence.state: decided and implemented at feature 2026-0812-14054p; reverses the earlier channel-adaptive constraints delivery (Claude `@import` + Codex pointer). Accepted trade-off: constraints lose guaranteed in-context presence and become a strong mandatory pointer on both channels. Supersedes the merge-conflict rationale in [[D-2026-0812-14054p-1]] (ids are now also filenames).
- provenance: distill @implement-tasks, 2026-08-12, derivation: explicit
- status: candidate
