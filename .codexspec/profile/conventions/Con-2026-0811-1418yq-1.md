## Con-2026-0811-1418yq-1: Adding a distributed command requires lockstep count/doc updates

- claim: When adding a new distributed slash command, update all of these together or a check drifts: (1) a `get_commands_metadata()` entry in `src/codexspec/commands/installer.py`; (2) the total AND the per-category count in that function's docstring (e.g. `git (2) -> git (3)`); (3) the inline `# <Category> Commands (N)` comment for the category — CAUTION its wording VARIES: it is `# Git Workflow Commands (N)` and `# Code Review Commands (N)`, NOT `# Git Commands`/`# Review Commands`, so a naive `grep '# Git Commands'` finds nothing and can FALSELY conclude the category has no inline comment (every category, including git and review, does have one); (4) the command-count assertions in BOTH `tests/commands/test_installer.py` (the total AND the per-category `len(... == N)`) AND `tests/test_cli.py` (the `"N"` in list-commands output); (5) a row in all 8 `README*.md` files (translated per language). The derived `.claude/commands/` and `.agents/skills/` artifacts are regenerated by `codexspec init` (publish → init), never hand-edited. Also: installer descriptions are zh-CN — a long one can trip ruff `E501` (ruff counts each CJK char as width 1, but ~90+ chars still overflows the 120 line budget); keep it short.
- type: convention
- scope/when: adding a new command template under `templates/commands/` in the codexspec repo
- consolidation: candidate; cluster: enumerated-set-lockstep
- evidence.facts: The inline comment `# Enhanced Commands (4)` was already stale (actual enhanced count was 6) before this feature; the docstring count and two separate test files (`test_installer.py`, `test_cli.py`) each carried an independent `20`/`6` assertion that had to be bumped; the `test_cli.py` one was only caught when the full suite failed after the others were fixed.
- evidence.state: observed at feature 2026-0811-1418yq-debug-command (adding `debug`), base commit 8e69f51. Verified — full suite green (1064 passed) only after every count site was updated in lockstep. RE-CONFIRMED at feature 2026-0813-1143el-release-notes (adding `release-notes` to the `git` category): review-plan wrongly reported "git has no inline count comment" because its `grep '# .* Commands (N)'` for the token `Git` missed `# Git Workflow Commands (2)`; the comment DID exist and needed `(2) -> (3)`. The zh-CN description also tripped ruff `E501` at 122 until shortened. Full suite green (1146 passed) after all sites updated in lockstep.
- provenance: distill @implement-tasks, 2026-08-11 (updated 2026-08-13), derivation: inferred
- status: candidate
