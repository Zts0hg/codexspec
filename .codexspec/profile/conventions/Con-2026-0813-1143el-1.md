## Con-2026-0813-1143el-1: A new commit-language-family command must join the `commit_templates` set; `init --force` safely regenerates derived

- claim: (A) `tests/test_sdd_workflow_templates.py::test_command_templates_split_interaction_and_document_language` splits command templates into two language regimes: templates in the `commit_templates` set (`commit-staged`, `pr`, now `release-notes`) must reference `language.commit`; EVERY OTHER template must reference BOTH `language.interaction` AND `language.document`. So when you add a new git/delivery-family command whose GENERATED CONTENT follows the `language.commit` priority (modeled on `pr.md`), you MUST add its name to that `commit_templates` set, or the test fails demanding the interaction/document block. This is a distribution-surface lockstep site NOT listed in [[Con-2026-0811-1418yq-1]] and it only applies to commit-language-family commands. (B) `uv run codexspec init . --force --ai both` regenerates the derived `.claude/commands/codexspec/<cmd>.md` + `.agents/skills/codexspec-<cmd>/SKILL.md` for the new command AND is SAFE to run mid-feature: `init` never fully regenerates `config.yml` even under `--force` — it preserves every key you did not pass (verified: `workflow.auto_next`, `workflow.auto_distill`, `language.interaction`, `document`, `project.ai=both` all survived), and it leaves already-in-sync derived files, `CLAUDE.md`/`AGENTS.md` managed blocks, and `constitution.md` untouched.
- type: convention
- scope/when: adding a new distributed command to the codexspec repo — part (A) only when its output language follows `language.commit` (git/delivery family); part (B) whenever regenerating derived install artifacts during a feature
- evidence.facts: full suite failed with `test_command_templates_split_interaction_and_document_language` → "release-notes must reference language.interaction in its Language Preference block" because release-notes was authored with the `pr.md` commit-priority block but was absent from `commit_templates = {"commit-staged", "pr"}`; adding `"release-notes"` to that set (design-directed: REQ-009 says its content follows `language.commit` "per pr.md") turned it green. After `codexspec init . --force --ai both`, `git status` showed only the new `release-notes` derived files as untracked and every other derived artifact + `config.yml` + `constitution.md` unchanged.
- evidence.state: observed at feature 2026-0813-1143el-release-notes, base commit 84b80b4. Verified — full suite green (1146 passed) and `git status` confirmed no config/derived churn. Complements [[Con-2026-0811-1418yq-1]] (the always-required count/doc sites) and [[P-2026-0812-2114vj-1]] (derived regen for chain-successor changes); this is the language-regime site + the init-safety fact.
- provenance: distill @implement-tasks, 2026-08-13, derivation: inferred
- status: candidate
